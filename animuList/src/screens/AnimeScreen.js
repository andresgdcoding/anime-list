import React from 'react';
import { View, StyleSheet, Text} from 'react-native';

const AnimeScreen = () => {
  return <Text style = {{fontSize: 40}}>AnimeScreen</Text>
};

const styles = StyleSheet.create({});

export default AnimeScreen;

